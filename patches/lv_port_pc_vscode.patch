diff --git a/CMakeLists.txt b/CMakeLists.txt
index 61e5947..5594205 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -67,9 +67,14 @@ add_compile_definitions($<$<BOOL:${LV_USE_FFMPEG}>:LV_USE_FFMPEG=1>)
 
 # Add LVGL subdirectory
 add_subdirectory(lvgl)
-target_include_directories(lvgl PUBLIC ${PROJECT_SOURCE_DIR} ${SDL2_INCLUDE_DIRS})
+target_include_directories(lvgl PUBLIC $<BUILD_INTERFACE:${PROJECT_SOURCE_DIR}> ${SDL2_INCLUDE_DIRS})
 
-set(MAIN_SOURCES src/mouse_cursor_icon.c src/hal/hal.c)
+# The Claude face sources (and the design's bitmap fonts they need) live in
+# apps/arduino/src/Claudy, not in this checkout, so `make link-faces`
+# symlinks them into src/faces.
+file(GLOB FACES_SOURCES ${PROJECT_SOURCE_DIR}/src/faces/*.cpp)
+
+set(MAIN_SOURCES src/mouse_cursor_icon.c src/hal/hal.c ${FACES_SOURCES})
 set(MAIN_LIBS lvgl lvgl::examples lvgl::demos lvgl::thorvg ${SDL2_LIBRARIES})
 
 # Create the main executable, depending on the FreeRTOS option
@@ -143,6 +148,7 @@ endif()
 
 add_executable(main ${MAIN_SOURCES})
 target_compile_definitions(main PRIVATE LV_CONF_INCLUDE_SIMPLE)
+target_include_directories(main PRIVATE ${PROJECT_SOURCE_DIR}/src/faces)
 target_link_libraries(main ${MAIN_LIBS})
 
 # On Windows, GUI applications do not show a console by default,
diff --git a/lv_conf.h b/lv_conf.h
index ee4aefe..c0afa62 100644
--- a/lv_conf.h
+++ b/lv_conf.h
@@ -204,7 +204,7 @@
 #define LV_USE_SNAPSHOT 0
 
 /** Backend that gives the SW renderer vector graphics support. */
-#define LV_USE_THORVG 0
+#define LV_USE_THORVG 1
 
 #if LV_USE_THORVG
 /** Build the ThorVG copy shipped with LVGL instead of linking an external one. */
diff --git a/src/main.c b/src/main.c
index c893220..5a45285 100644
--- a/src/main.c
+++ b/src/main.c
@@ -25,6 +25,7 @@
 #include <SDL.h>
 
 #include "hal/hal.h"
+#include "claude_face_v2.h"
 
 /*********************
  *      DEFINES
@@ -61,15 +62,17 @@ int main(int argc, char **argv)
   lv_init();
 
   /*Initialize the HAL (display, input devices, tick) for LVGL*/
-  sdl_hal_init(320, 480);
+  sdl_hal_init(CF_DISPLAY_SIZE, CF_DISPLAY_SIZE);
 
-  /* Run the default demo */
-  /* To try a different demo or example, replace this with one of: */
+  /* Show the Claude Code status face instead of a stock LVGL demo. */
+  /* To try a stock demo/example again, replace this with one of: */
+  /* - lv_demo_widgets(); */
   /* - lv_demo_benchmark(); */
   /* - lv_demo_stress(); */
   /* - lv_example_label_1(); */
   /* - etc. */
-  lv_demo_widgets();
+  claude_face_create(NULL);
+  claude_face_cycle(true);
 
   while(1) {
     /* Periodically call the lv_task handler.
